# 19-案例-多级对象解构

## 1. 需求

- 设计一个函数,该函数接收`msg`,解构其中的`data`字段,重命名为`myData`变量并返回
- 设计一个函数,调用该函数时,传递实参`msg`,但是在函数体中,可以直接访问到`msg.data`字段
- 基于上述需求,将`data`变量命名为`myData`

## 2. 实现

- 可以在函数形参列表中,直接解构`msg`对象,并重命名`data`字段为`myData`