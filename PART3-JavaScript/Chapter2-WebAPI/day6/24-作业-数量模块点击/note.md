# 24-作业-数量模块点击

## 1. 需求

- 点击加减可以修改商品数量
- 如果购买数量为1,则鼠标提示禁用提示
- 直接输入购买数量时:
  - 若输入的值为0,则提示不得少于1件
  - 若输入的值不是数字,则提示必须输入数字

## 2. 实现

### 2.1 点击增加

- step1. 计数+1
- step2. 若减少按钮禁用,则移除禁用

### 2.2 点击减少

- step1. 若当前数值为1,设置禁用后返回即可
- step2. 计数-1

### 2.3 直接输入

- step1. `change`事件,而非`blur`事件