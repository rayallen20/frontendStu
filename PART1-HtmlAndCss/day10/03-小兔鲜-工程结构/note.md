# 03-小兔鲜-工程结构

```
tree ./ -L 1
./
├── css
├── favicon.ico
├── iconfont        // 字体图标
├── images          // 固定使用的图片
├── index.html
└── uploads         // 后台上传的图片(可变的)

4 directories, 2 files
```

```
tree ./css/
./css/
├── base.css        // 基础公共样式
└── index.css       // 首页CSS样式

0 directories, 2 files
```