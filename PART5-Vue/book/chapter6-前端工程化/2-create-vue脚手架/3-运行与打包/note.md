# 3-运行与打包

```json
{
  "scripts": {
    "dev": "vite",            // 开发环境,启动开发服务器.`vite`是`vite dev`或`vite serve`的别名
    "build": "vite build",    // 生产环境,构建生产应用
    "preview": "vite preview" // 本地预览生产环境构建结果
  }
}
```