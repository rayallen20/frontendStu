# 04-v-model指令

- `v-model`指令的本质是个语法糖:
  - 使用`v-bind`指令为元素的`value`属性绑定变量
  - 使用`v-on`指令为元素绑定`input`事件,并在事件回调函数中重新为`value`属性绑定的变量赋值

## 1. 基本使用

## 2. 实现原理

## 3. 绑定其他表单元素

## 4 `v-model`表单元素的value属性绑定

## 5. `v-model`修饰符